name: project-env
channels:
  - conda-forge
  - defaults
dependencies:
  - ipykernel
  - python>=3.10
  - scikit-learn # sklearn is an alias for scikit-learn
  - jupyterlab
prefix: /opt/miniconda3/envs/dstoolbox
